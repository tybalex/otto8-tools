---
Name: list_database_tables
Description: List all tables in the SQLite database and return a JSON object containing the results

#!${GPTSCRIPT_TOOL_DIR}/bin/gptscript-go-tool listDatabaseTables

---
Name: list_database_table_rows
Description: List all rows from the specified table in the SQLite database and return a JSON object containing the results
Param: table: Name of the table to list rows from

#!${GPTSCRIPT_TOOL_DIR}/bin/gptscript-go-tool listDatabaseTableRows